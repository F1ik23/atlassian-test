import { TableCell } from "@mui/material";
import { styled } from "@mui/material/styles";

export const HeadStyle = styled(TableCell)(() => ({
    backgroundColor: 'grey', 
    color: 'white', 
    textAlign: 'center',
    border: '1px solid white'
}))